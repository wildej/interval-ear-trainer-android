# Product backlog

## Idea

1. Нужно добавить еще один режим, назовем его игровой: на экране два столбца. В левом несколько кнопок "прослушать интервал", лучше всего если на них будет графическое изображение записи. В правом - соответствующее им количество кнопок с названием интервалов. Порядок перемешанный случайно.
Задача игрока по очереди нажимая кнопки справа и слева выбирать соответствующие прослушанному интервалу
При ошибке оповещать об этом, и давать пользователю снова выбрать кнопку в правом столбце (левый считаем уже выбранным)

2. Нужно добавить маскота (картинка маскота уже есть). Пусть он сообщает об ошибках и правильных ответах
Так же можно добавить игровых моментов: радоваться 3 или 5 или 10 правильным ответам подряд например

## Priority 1

- **Сделано:** `applicationId` / `namespace` — `com.muxaeji.intervalo`, исходники в `com.muxaeji.intervalo.*`.
- **Сделано:** публикация кода под **MIT** — [`LICENSE`](LICENSE); сторонние компоненты — [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md).

## Priority 2

### Audio

- **Сделано:** SoundFont `Chorium.SF2` в `assets/soundfonts/`, рендер через FluidSynth ([fluidsynth-kmp](https://github.com/kotlinds/fluidsynth-kmp)), см. `SoundFontIntervalAudioPlayer`.
- **Запасные пути:** WAV Teddy_Frost → одиночный `piano_ref.wav` → синус (`IntervalAudioPlayerProvider`).
- **Опционально позже:** другой SF2 (больше GM-покрытие, другой тембр) — подменить файл в `assets/soundfonts/` и при необходимости preset/program в плеере.

### Playback timing

- **Зафиксировано:** текущие значения в `IntervalPlaybackTiming` оставляем без дальнейшей настройки (smoke ок).

- Нужна темная тема, светлая неудобная

### UX тренировки

- **Сделано:** ответ фиксируется по нажатию на вариант интервала; отдельная кнопка «Проверить» не используется; переход к следующему заданию — «Дальше».

## Priority 3

### Термины и формулировки

- **Сделано:** «сессия» заменена на «тренировка» в пользовательских строках; в сообщении о результате показывается полное название интервала (и краткое в скобках).
